feat:
  + otel trace(由于是拆分组件的形式, 目前我们也许并不需要构建trace配置)
    + 我们可以尝试使用trace, 将ctx传递在ir中
  + otel metrics √
  + otel collector feature √
  + configMap configuration Jaeger-operator
  + 以上是相关联的

  + distribute strategy √
  + component config
  + tls connection
  + k8s event
  + ginkgo test
  + chart
  
fix:
  + README.md
  + config file
  + github Action
  + makefile 一半一半

refactor:
  + translator
  + controller
  + infra
  + k8s condition